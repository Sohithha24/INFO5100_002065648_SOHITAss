public class Square extends Rectangle {

    // Constructor (Square is a special Rectangle where all sides are equal)
    public Square(double side) {
        super(side, side); // Pass the same value for both length and width
    }
}

